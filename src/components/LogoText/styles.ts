import styled from 'styled-components';

export const Logo = styled.h1`
  font-style: normal;
  font-weight: 600;
  font-size: 28px;
  line-height: 29px;
  color: var(--primary);
  margin-right: 24px;
`;
